const fs = require("fs"); const path = require("path"); const sharp = require("sharp"); const imgDir = path.join(__dirname, "frontend", "img"); async function processDir(dir) { if (!fs.existsSync(dir)) return; const files = fs.readdirSync(dir); for (const file of files) { const fullPath = path.join(dir, file); if (fs.statSync(fullPath).isDirectory()) { await processDir(fullPath); } else if (/\.(jpg|jpeg)$/i.test(fullPath)) { const tmp = fullPath + ".tmp"; try { await sharp(fullPath).resize(800, null, { withoutEnlargement: true }).jpeg({ quality: 45, force: true }).toFile(tmp); fs.unlinkSync(fullPath); fs.renameSync(tmp, fullPath); console.log("Compressed:", file); } catch (e) { console.log(e.message) } } else if (/\.(png)$/i.test(fullPath)) { const tmp = fullPath + ".tmp"; try { await sharp(fullPath).resize(800, null, { withoutEnlargement: true }).png({ quality: 45, force: true }).toFile(tmp); fs.unlinkSync(fullPath); fs.renameSync(tmp, fullPath); console.log("Compressed:", file); } catch (e) { console.log(e.message) } } } } processDir(imgDir).then(() => console.log("Done compressing!"));
